# DEPRECATED — 勿再维护

本目录下的 `*.yaml` **不再**作为 TECH API 权威契约。

请改用：

1. 后端仓库中 API 接口 + SpringDoc 注解（位置按项目实际）  
2. `generate-api-doc` skill 导出的 `docs/api-doc/*.html`  
3. TECH-DESIGN §5 列表 + §7「后端原型契约」表  

复制 `docs/template/tech/` 到 Epic 时：**可删除整个 `openapi/` 目录**，或保留本 README 作提示即可。
