# 技术方案增量：<迭代一句话摘要>

> 文档类型：Technical Design Delta（迭代变更视图）  
> 适用目录：`docs/epics/<epic-id>-<slug>/`  
> 文件名约定：`TECH-DESIGN-delta-v{版本}-{kebab-slug}.md`（例：`TECH-DESIGN-delta-v0.2-content-summary.md`）  
> Tech ID：`T-<领域>-<序号>`（与全量 TECH 一致）  
> Epic ID：`E-<领域>-<序号>`  
> 全量 TECH：`TECH-DESIGN.md`（同目录；**聚合后权威**）  
> 基线版本：vX.Y（本迭代合并前的全量 TECH 版本）  
> 本版版本：vX.Z（与全量 TECH 文首版本一致）  
> 覆盖 PRD：见 §1（**一份增量可对应多个 PRD**）  
> 关联卡号：<卡号>  
> 状态：Draft / Ready / Implementing / Done  
> 作者：<研发负责人>  
> 日期：YYYY-MM-DD  
> 模版：`docs/template/技术方案增量模版.md`  
> 体系说明：`docs/README-PRD体系.md`

---

## 0. 本迭代摘要

### 0.1 一句话

<相对基线，本迭代要交付的技术能力。>

### 0.2 In / Out

| 类型 | 内容 |
|------|------|
| In | <本迭代必做：字段 / API / DDL / 行为…> |
| Out | <本迭代明确不做> |

### 0.3 权威与冲突

| 内容 | 权威来源 |
|------|----------|
| 当前全量契约（聚合后） | `TECH-DESIGN.md` + `tech/` |
| **本迭代改了什么** | **本文** |
| 业务规则 / AC | 相关 PRD + `shared-context.md` |
| HTTP 字段级 | API 接口 + SpringDoc / 生成 HTML（若有） |

冲突：全量 TECH + `tech/` > 本文摘要；发现不一致 → **先回写全量再改本文**。

---

## 1. 影响 PRD

| PRD ID | 标题 | 本迭代覆盖点（实现视角） |
|--------|------|--------------------------|
| P-xxx-01 | <标题> | <…> |
| P-xxx-02 | <标题> | <…>（可多行） |

---

## 2. 字段 / 领域变更

| 变更 | 对象 | 说明 | 落点（表列 / 领域类） |
|------|------|------|----------------------|
| ADD / MODIFY / REMOVE | `<Type.field>` | <语义一句> | `<table.column>` / `…/Foo.java` |

无则写「无」。

---

## 3. API / 事件变更

> 只列**相对基线有变更**的行；未改接口不要抄全量表。

| 类型 | Path / Event | Method | 变更类型 | 关联 PRD | 备注 |
|------|--------------|--------|----------|----------|------|
| HTTP | `/api-…` | PUT | MODIFY | P-xxx | <…> |
| MQ | `com.example.events.…` | MQ | MODIFY | P-xxx | <…> |

无则写「无」。

---

## 4. DDL / 持久化

| 脚本 / 对象 | 变更 | 说明 |
|-------------|------|------|
| `tech/ddl/1xx_….sql` | ADD COLUMN `…` | <…> |

无则写「无」。**禁止**在此粘贴全表 DDL；细节链 `tech/ddl/`。

---

## 5. 行为 / 规则变更

| 规则 / 行为 | 相对基线的差异 | 引用 |
|-------------|----------------|------|
| <例：Scheduler 扫描点> | `startAt` → `T_push=COALESCE(…)` | E-R-008 |

无则写「无」。

---

## 6. 原型契约（可选）

| 项 | 值 |
|----|-----|
| 前端仓库 / 分支 / path | <或「无本迭代前端」> |
| 后端仓库 / 分支 | |
| 领域 / UseCase 接口 path | |
| Controller / DTO path | |
| API 文档 HTML | `docs/api-doc/…`（若已导出） |
| 状态 | Draft / 已导出 |
| 范围说明 | **仅契约**；Impl / 落库见 §7 |

---

## 7. 实现清单（供 writing-plans）

- [ ] DDL / 迁移脚本（Liquibase 等按项目实际）落地  
- [ ] 领域校验 / UseCaseImpl  
- [ ] Repository / Mapper / Entity  
- [ ] Scheduler / MQ（若涉及）  
- [ ] 单测 / api-test  
- [ ] 前端联调（若涉及）  

---

## 8. 回写全量记录

| 全量 TECH / tech 位置 | 已合并内容 |
|-----------------------|------------|
| `TECH-DESIGN.md` §x | <…> |
| `tech/domain.puml` | <…> |
| Epic §0.1 Tech 索引 | 已链本文 |

---

## 写作约束

1. **短**：读者应在数分钟内看完「本轮改什么」。  
2. **不抄全量**：无变更章节写「无」或省略小节。  
3. **先全量后增量**：领域/表/API 的聚合结果写入 `TECH-DESIGN.md` + `tech/`；本文只保留差集与实现勾选。  
4. **多 PRD**：同一版本号一份 delta；§1 列出全部覆盖 PRD。  
5. Agent：`.agents/skills/epic-prd-tech-design-workflow/tech-design-author/SKILL.md`（增量流程）。
