# 共享上下文：<Epic 名称>

> 文档类型：Epic Shared Context（本 Epic 单一事实源）  
> 所属 Epic：`EPIC.md`（同目录）  
> Epic ID：`E-<领域>-<序号>`  
> 版本：v0.1（可与 EPIC.md 版本独立演进；重大语义变更须同步检查下属 PRD）  
> 路径约定：`docs/epics/<epic-id>-<slug>/shared-context.md`  
> 全局共享上下文：`docs/epics/global-share-context.md`（跨 Epic；必引用）  
> 体系说明：`docs/README-PRD体系.md`

> 下属 PRD **只引用、不重新定义**本文与 global 已定义内容。  
> 冲突：`global-share-context.md` > 本文 > PRD（针对同一术语/对象）。

---

## 0. 引用全局共享上下文

本 Epic 复用的跨 Epic 能力（只列引用，不重定义）：

| 能力 / 术语 | 见 global 章节 |
|-------------|----------------|
| <例：CouponStock / Coupon / CouponClaimSlot> | `global-share-context.md` §2.1 |
| <无则写「本 Epic 暂无额外 global 依赖」> | — |

---

## 1. 术语表

| 术语 | 术语英文 | 定义 | 备注 |
|------|----------|------|------|
| <术语> | <EnglishTerm> | <定义；若已在 global 登记则写「见 global §x」> | <易混说明；无则空> |
| 权限码 | Permission Code | 系统能力控制标识；由管理员分配给角色 | PRD 以权限码描述能力，不枚举角色 |

---

## 2. 共享权限码

> 系统以权限码控制能力；角色仅作为权限码的承载容器（由管理员配置），**规格文档不定义、不绑定具体角色**。

| 权限码 | 说明 | 典型能力 |
|--------|------|----------|
| `<PERMISSION_CODE_A>` | <含义> | <如：查看> |
| `<PERMISSION_CODE_B>` | <含义> | <如：新建 / 编辑 / 发布> |

---

## 3. 共享业务对象

| 业务对象 | 业务对象英文 | 说明 | 关键状态概念（可选） | 归属关系 |
|----------|--------------|------|----------------------|----------|
| <对象 A> | <EnglishNameA> | <说明> | <如 draft/published> | <与其他对象关系> |
| <对象 B> | <EnglishNameB> | <说明> | <无则「无」> | <关系> |

> 跨 Epic 已有对象（如 `CouponStock`）勿在此重新定义全文；写「见 global」并说明本 Epic 如何引用即可。

---

## 4. 共享权限原则

- EP-001：是否允许某操作，以请求方是否持有对应**权限码**为准，不以角色名为准。
- EP-002：未持有所需权限码时，系统拒绝操作并返回明确错误。
- EP-003：<其他全局原则，如数据范围、跨类型是否隔离等>

---

## 5. 跨 PRD 约束

> 无跨 PRD 约束时，本节写「无」。跨 Epic 约束引用 `G-R-xxx`，不在此改写。

E-R-001：<约束名称>

- 适用：下属全部 PRD / 指定 P-xxx
- 条件：<触发条件>
- 动作：<必须遵守的系统或业务行为>
- 说明：PRD 内规则可 `引用：E-R-001` 或 `引用：G-R-001`，不得改写含义
