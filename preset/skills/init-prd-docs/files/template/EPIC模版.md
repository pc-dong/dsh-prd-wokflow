# Epic：<Epic 名称>

> 文档类型：Epic  
> 适用目录：`docs/epics/<epic-id>-<slug>/`  
> Epic ID：`E-<领域>-<序号>`  
> 版本：v0.1  
> 状态：Draft / Active / Done  
> 作者：<BA/产品/负责人>  
> 日期：YYYY-MM-DD  
> 关联项目 / 卡号：<可选；无则填「无」>  
> 共享上下文：`shared-context.md`（同目录；必填）  
> 全局共享上下文：`docs/epics/global-share-context.md`  
> 技术方案：`TECH-DESIGN.md` + `tech/`（同目录；进入实现前建议必填）  
> 体系说明：`docs/README-PRD体系.md`

---

## 0. 下属 PRD 索引

| PRD ID | 标题 | 一句话范围 | 路径 | 状态 |
|--------|------|------------|------|------|
| P-xxx-01 | <标题> | <子场景一句话> | `prds/P-xxx-01-<slug>.md` | Draft |
| P-xxx-02 | <标题> | <子场景一句话> | `prds/P-xxx-02-<slug>.md` | Draft |

### 0.1 技术方案

| Tech ID | 路径 | 状态 |
|---------|------|------|
| T-<同 Epic> | `TECH-DESIGN.md` | Draft / Ready / … |

### 0.2 前端原型（可选）

> 由 `frontend-prototype` skill 回写；无则填「未做」。

| 项 | 值 |
|----|-----|
| 前端仓库 | （按项目实际） |
| 分支 | `feature/<卡号>-…` |
| 主要 path | `src/...` |
| 启动 | `npm run dev`（改已有菜单时 + auth-mock） |
| 状态 | 未做 / Draft / 已对齐 |

---

## 1. 背景与业务动机

<说明为什么要做本 Epic。不要写字段、接口、UI。>

---

## 2. 业务目标与成功标准

### 2.1 目标

- <目标 1>
- <目标 2>

### 2.2 成功标准

| 编号 | 标准 | 衡量方式 |
|------|------|----------|
| GS-001 | <完成后应达到的业务效果> | <指标或定性验收> |
| GS-002 | <完成后应达到的业务效果> | <指标或定性验收> |

---

## 3. 范围边界

### 3.1 包含（In）

- <范围 1>
- <范围 2>

### 3.2 不包含（Out）

- <明确排除项 1>
- <明确排除项 2>

### 3.3 与相邻 Epic 的边界

- <与 E-xxx 的边界说明；无则填「无」>

---

## 4. 用户旅程

> 每步格式：角色 → 意图 → 触点/系统 → 可观察结果。  
> 可标注 `→ 详见 P-xxx`。不要在本节省写字段校验与异常码。

### 4.1 主旅程

1. <角色> → <意图> → <触点/系统> → <可观察结果> → 详见 P-xxx
2. <...>
3. <...>

### 4.2 关键分支

1. <分支描述> → 详见 P-xxx
2. <...>

### 4.3 旅程示意（可选）

```mermaid
flowchart LR
  A[步骤1] --> B[步骤2] --> C[步骤3]
```

---

## 5. 里程碑与依赖

| 里程碑 | 意图 | 依赖 | 关联 PRD |
|--------|------|------|----------|
| M1 | <交付意图> | <外部依赖；无则「无」> | P-xxx-01 |
| M2 | <交付意图> | <外部依赖；无则「无」> | P-xxx-02 |

---

## 6. 共享上下文引用

术语、共享权限码、业务对象、权限原则、跨 PRD 约束（`E-R-xxx`）统一写在同目录：

- 路径：`shared-context.md`
- 模版：`docs/template/EPIC-共享上下文模版.md`

本文件不重复定义共享内容；冲突以 `shared-context.md` 为准。

---

## 7. AI 消费提示

1. 先读本 Epic，再读同目录 `shared-context.md`，再读目标 PRD；未点名的 PRD 默认不读。
2. 实现时不得发明与 `shared-context.md` 中权限码 / 对象 / 术语冲突的新定义。
3. PRD 与 `shared-context.md` 冲突 → 停止实现，并在计划中记录冲突。
4. 仅当目标 PRD 声明了 `stories_appendix` 时才读取附录。
5. 不得根据 §4 旅程步骤自行补全 PRD 未写的业务规则。
6. 体系约定见 `docs/README-PRD体系.md`。
