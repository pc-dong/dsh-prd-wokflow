# API Doc（工作区根）

本目录存放 SpringDoc 导出脚本与生成的 **HTML**（不再生成 PDF）。HTML 为生成产物，不入库；需要时用下方脚本重新导出。

- 工作目录：`docs/api-doc/`（相对工作区根）
- 启动目标 app 仍在 `backend-service`（`bootRun`）
- 环境地址：脚本内 `https://api.sit.example.com` 为示例占位，按实际环境替换
- 用法见 `.agents/skills/generate-api-doc/SKILL.md`
